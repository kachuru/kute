#!/bin/bash
if [ ! -n "$1" ]
then
    echo "Usage ./jump <remote_host>"
else
    ssh -A $1 "curl --silent --show-error --header 'x-connect-key: ${JUMPCLOUD_CONNECT_KEY}' https://kickstart.jumpcloud.com/Kickstart | bash"
fi
